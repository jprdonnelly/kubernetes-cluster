#This setting enables dev mode to include a local MongoDB install
devMode:
  enabled: true

#This setting accepts the EULA for the product
engine:
  acceptEULA: "yes"

identity-providers:           
  secrets:
    idpConfigs:
      - discoveryUrl: "https://djx.auth0.com/.well-known/openid-configuration"
        primary: true
        clientId: "5kohQL0166VN7rcEv7P7li0gWPBjbiZo"
        clientSecret: "NLwAqaeyGr83xl19Xb9lBXjt5ra1U_-5LKuFwxBnzvSsqmAUXaT6T7BeQGgIRYOd"
        realm: "Auth0"
        hostname: "192.168.205.90"
        claimsMapping:
          client_id: [ "client_id", "azp" ]
          groups: "/https:~1~1qlik.com~1groups"
          sub: ["/https:~1~1qlik.com~1sub", "sub"]

qix-datafiles:
  persistence:
    accessMode: ReadWriteMany
    enabled: true
    existingClaim: qseok-vol
redis:
  persistence:
    accessMode: ReadWriteMany
    enabled: true
    existingClaim: qseok-vol
reporting:
  persistence:
    accessMode: ReadWriteMany
    enabled: true
    existingClaim: qseok-vol
resource-library:
  persistence:
    accessMode: ReadWriteMany
    enabled: true
    existingClaim: qseok-vol
temporary-contents:
  persistence:
    accessMode: ReadWriteMany
    enabled: true
    existingClaim: qseok-vol
engine:
  acceptEULA: "yes"
  persistence:
    accessMode: ReadWriteMany
    enabled: true
    existingClaim: qseok-vol
dcaas:
  dcaas-redis:
    persistence:
      accessMode: ReadWriteMany
      enabled: true
      existingClaim: qseok-vol